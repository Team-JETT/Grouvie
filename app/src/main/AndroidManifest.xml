<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jett_apps.grouvie">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SelectFilm"
            android:label="@string/title_activity_select_film"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.SelectShowtime" />
        <activity android:name=".Views.LeaderInitialPlan" />

        <meta-data
            android:name="net.hockeyapp.android.appIdentifier"
            android:value="${HOCKEYAPP_APP_ID}" />

        <activity android:name=".Activities.SelectCinema" />
        <activity
            android:name=".Activities.SelectGroup"
            android:label="@string/title_activity_group_selection"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Views.LandingPage" />
        <activity android:name=".Views.CurrentPlanView" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Views.ProfilePage" />
        <activity android:name=".Views.GroupView" />
        <!-- <activity android:name=".SuggestChange" /> -->
        <activity android:name=".SuggestionActivities.SuggestChangeInPlan" />
        <activity android:name=".SuggestionActivities.SuggestFilmChange" />

        <service android:name=".Notifications.IIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".Notifications.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".SuggestionActivities.SuggestCinemaChange" />
        <activity android:name=".SuggestionActivities.SuggestShowtimeChange"></activity>
    </application>

</manifest>
